const Discord = require("discord.js"); 

module.exports = class lenguage {
    constructor() {
        this.name = "lenguage",
        this.alias = [],
        this.usage = "a!lenguage"
    }

    async run(client, message, args) {
        try{
          
        }catch(e) {
            throw e;
        }
    }
}